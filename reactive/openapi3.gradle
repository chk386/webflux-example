openapi3 {
  server = "http://localhost:8080"
  title = "Spring5.2 Reactive Webflux API"
  version = 1.0
  description = """
                |Webflux API Document
                """.stripMargin()
  format = 'yml'
  separatePublicApi = false
  outputFileNamePrefix = project.name
  outputDirectory = project.ext.get("snippetsDir") + "/openapi3"
}